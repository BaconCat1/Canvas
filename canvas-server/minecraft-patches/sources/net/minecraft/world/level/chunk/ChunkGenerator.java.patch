--- a/net/minecraft/world/level/chunk/ChunkGenerator.java
+++ b/net/minecraft/world/level/chunk/ChunkGenerator.java
@@ -229,7 +_,7 @@
             int chunkOriginX = SectionPos.blockToSectionCoord(pos.getX());
             int chunkOriginZ = SectionPos.blockToSectionCoord(pos.getZ());
 
-            for (int radius = 0; radius <= maxSearchRadius; radius++) {
+            for (int radius = (io.canvasmc.canvas.GlobalConfiguration.getInstance().chunkSystem.optimizeTreasureMapLocating ? maxSearchRadius : 0); radius <= maxSearchRadius; radius++) { // Canvas - optimize treasure map locating - just search the largest radius to avoid spamming iterations
                 boolean foundSomething = false;
 
                 for (Entry<StructurePlacement, Set<Holder<Structure>>> entry : randomSpreadEntries) {
@@ -536,6 +_,17 @@
         final Level level, final StructureManager structureManager, final MobCategory mobCategory, final BlockPos pos
     ) {
         Map<Structure, LongSet> structures = structureManager.getAllStructuresAt(pos);
+
+        return this.getMobsAt(level, structureManager, mobCategory, pos, structures); // Canvas - reuse known structure references
+    }
+
+    public WeightedList<MobSpawnSettings.SpawnerData> getMobsAt(
+        final Level level,
+        final StructureManager structureManager,
+        final MobCategory mobCategory,
+        final BlockPos pos,
+        final Map<Structure, LongSet> structures
+    ) {
 
         for (Entry<Structure, LongSet> entry : structures.entrySet()) {
             Structure structure = entry.getKey();

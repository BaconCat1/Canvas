--- a/net/minecraft/world/entity/LivingEntity.java
+++ b/net/minecraft/world/entity/LivingEntity.java
@@ -297,6 +_,8 @@
     public @Nullable List<DefaultDrop> deathDropItems = null;
     public final org.bukkit.craftbukkit.attribute.CraftAttributeMap craftAttributes;
     public boolean collides = true;
+    public boolean canvas$fromSpawner = false; // Canvas - spawner configs
+    protected boolean canvas$isDyingFromWitherRose = false; // Canvas - fix Folia#388
     public Set<UUID> collidableExemptions = new java.util.HashSet<>();
     public boolean bukkitPickUpLoot;
     public boolean silentDeath = true; // Paper - mark entity as dying silently for cancellable death event
@@ -421,7 +_,7 @@
         if (this.level() instanceof ServerLevel level && onGround && this.fallDistance > 0.0) {
             this.onChangedBlock(level, pos);
             double power = Math.max(0, Mth.floor(this.calculateFallPower(this.fallDistance)));
-            if (power > 0.0 && !onState.isAir()) {
+            if (power > 0.0 && !(onState.isAir() || (io.canvasmc.canvas.GlobalConfiguration.getInstance().vanillaFixes.mc30391 && (this instanceof net.minecraft.world.entity.animal.chicken.Chicken || this instanceof net.minecraft.world.entity.monster.Blaze || this instanceof WitherBoss)))) { // Canvas - MC-30391
                 double x = this.getX();
                 double y = this.getY();
                 double z = this.getZ();
@@ -437,11 +_,13 @@
                 double scale = Math.min(0.2F + power / 15.0, 2.5);
                 int particles = (int)(150.0 * scale);
                 // CraftBukkit start - visibility API
-                if (this instanceof ServerPlayer) {
-                    level.sendParticlesSource(this, new BlockParticleOption(ParticleTypes.BLOCK, onState), false, false, x, y, z, particles, 0.0, 0.0, 0.0, 0.15F);
-                } else {
-                level.sendParticles(new BlockParticleOption(ParticleTypes.BLOCK, onState), x, y, z, particles, 0.0, 0.0, 0.0, 0.15F);
-                }
+                if (!level().canvasConfig().visuals.particles.disableFallParticles) { // Canvas - particles config
+                    if (this instanceof ServerPlayer) {
+                        level.sendParticlesSource(this, new BlockParticleOption(ParticleTypes.BLOCK, onState), false, false, x, y, z, particles, 0.0, 0.0, 0.0, 0.15F);
+                    } else {
+                        level.sendParticles(new BlockParticleOption(ParticleTypes.BLOCK, onState), x, y, z, particles, 0.0, 0.0, 0.0, 0.15F);
+                    }
+                } // Canvas - particles config
                 // CraftBukkit end - visibility API
             }
         }
@@ -482,7 +_,7 @@
         super.baseTick();
         if (this.isAlive() && this.level() instanceof ServerLevel level) {
             boolean isPlayer = this instanceof Player;
-            if (this.isInWall()) {
+            if (shouldCheckForSuffocation() && this.isInWall()) { // Pufferfish - optimize suffocation
                 this.hurtServer(level, this.damageSources().inWall(), 1.0F);
             } else if (isPlayer && !level.getWorldBorder().isWithinBounds(this.getBoundingBox())) {
                 double dist = level.getWorldBorder().getDistanceToBorder(this) + level.getWorldBorder().getSafeZone();
@@ -559,6 +_,19 @@
         this.yHeadRotO = this.yHeadRot;
         this.yBodyRotO = this.yBodyRot;
     }
+    // Pufferfish start - optimize suffocation
+
+    public boolean couldPossiblyBeHurt(final float amount) {
+        if ((float) this.getInvulnerableTime() > (float) this.invulnerableDuration / 2.0F && amount <= this.lastHurt) {
+            return false;
+        }
+        return true;
+    }
+
+    public boolean shouldCheckForSuffocation() {
+        return !level().canvasConfig().enableSuffocationOptimization || (tickCount % 10 == 0 && couldPossiblyBeHurt(1.0F));
+    }
+    // Pufferfish end
 
     protected boolean shouldTakeDrowningDamage() {
         return this.getAirSupply() <= -20;
@@ -587,9 +_,9 @@
     }
 
     protected void tryAddFrost() {
-        if (!this.getBlockStateOnLegacy().isAir()) {
+        if (true) { // Canvas - check frozen ticks before landing block
             int ticksFrozen = this.getTicksFrozen();
-            if (ticksFrozen > 0) {
+            if (ticksFrozen > 0 && !this.getBlockStateOnLegacy().isAir()) { // Canvas - check frozen ticks before landing block
                 AttributeInstance speed = this.getAttribute(Attributes.MOVEMENT_SPEED);
                 if (speed == null) {
                     return;
@@ -629,6 +_,7 @@
     public boolean broadcastedDeath = false; // Folia - region threading
     protected void tickDeath() {
         this.deathTime++;
+        if (level().canvasConfig().combat.removeRedDeathAnimation) this.deathTime = 20; // Canvas - remove red death animation
         if (this.deathTime >= 20 && !this.level().isClientSide() && !this.isRemoved()) {
             this.level().broadcastEntityEvent(this, EntityEvent.POOF); this.broadcastedDeath = true; // Folia - region threading - death has been broadcasted
             if (!(this instanceof ServerPlayer)) this.remove(Entity.RemovalReason.KILLED, org.bukkit.event.entity.EntityRemoveEvent.Cause.DEATH); // CraftBukkit - add Bukkit remove cause // Folia - region threading - don't remove, we want the tick scheduler to be running
@@ -788,7 +_,13 @@
 
         super.remove(reason, eventCause); // CraftBukkit
         this.brain.clearMemories();
-        this.getCombatTracker().recheckStatus();
+        // Canvas start - fix MC-298464 while retaining vanilla behavior when disabled
+        if (io.canvasmc.canvas.GlobalConfiguration.getInstance().vanillaFixes.mc298464) {
+            this.combatTracker.resetCombatState();
+        } else {
+            this.getCombatTracker().recheckStatus();
+        }
+        // Canvas end - fix MC-298464 while retaining vanilla behavior when disabled
     }
 
     @Override
@@ -846,6 +_,7 @@
         if (this.locatorBarIcon.hasData()) {
             output.store("locator_bar_icon", Waypoint.Icon.CODEC, this.locatorBarIcon);
         }
+        output.store("from_spawner", com.mojang.serialization.Codec.BOOL, this.canvas$fromSpawner); // Canvas - spawner configs
     }
 
     // Paper start - Extend dropItem API
@@ -940,8 +_,8 @@
         this.hurtTime = input.getShortOr("HurtTime", (short)0);
         this.deathTime = input.getShortOr("DeathTime", (short)0); this.broadcastedDeath = false; // Folia - region threading
         input.getString("Team").ifPresent(teamName -> {
-            Scoreboard scoreboard = this.level().getScoreboard();
-            PlayerTeam team = scoreboard.getPlayerTeam(teamName);
+            net.minecraft.world.scores.Scoreboard scoreboard = this.level().getScoreboard();
+            net.minecraft.world.scores.PlayerTeam team = scoreboard.getPlayerTeam(teamName);
             if (!this.level().paperConfig().scoreboards.allowNonPlayerEntitiesOnScoreboards && !(this instanceof net.minecraft.world.entity.player.Player)) { team = null; } // Paper - Perf: Disable Scoreboards for non players by default
             boolean success = team != null && scoreboard.addPlayerToTeam(this.getStringUUID(), team);
             if (!success) {
@@ -967,6 +_,7 @@
         this.locatorBarIcon = input.read("locator_bar_icon", Waypoint.Icon.CODEC).orElseGet(Waypoint.Icon::new);
         this.currentImpulseContextResetGraceTime = input.getIntOr("current_impulse_context_reset_grace_time", 0);
         this.currentImpulseImpactPos = input.read("current_explosion_impact_pos", Vec3.CODEC).orElse(null);
+        this.canvas$fromSpawner = input.read("from_spawner", com.mojang.serialization.Codec.BOOL).orElse(this.canvas$fromSpawner); // Canvas - spawner configs
     }
 
     @Override
@@ -1015,7 +_,7 @@
             }
 
             List<ParticleOptions> particles = this.entityData.get(DATA_EFFECT_PARTICLES);
-            if (!particles.isEmpty()) {
+            if (!particles.isEmpty() && !level().canvasConfig().visuals.particles.disableEffectParticles) { // Canvas - particles config
                 boolean isAmbient = this.entityData.get(DATA_EFFECT_AMBIENCE_ID);
                 int bound = this.isInvisible() ? 15 : 4;
                 int ambientFactor = isAmbient ? 5 : 1;
@@ -1046,14 +_,18 @@
     }
 
     private void updateSynchronizedMobEffectParticles() {
-        List<ParticleOptions> visibleEffectParticles = this.activeEffects
-            .values()
-            .stream()
-            .filter(MobEffectInstance::isVisible)
-            .map(MobEffectInstance::getParticleOptions)
-            .toList();
+        // Canvas start - remove streams
+        List<ParticleOptions> visibleEffectParticles = new java.util.ArrayList<>();
+        final Collection<MobEffectInstance> effectsValues = this.activeEffects.values(); // Canvas - optimized collection
+
+        for (MobEffectInstance effect : effectsValues) { // Canvas - optimized collection
+            if (effect.isVisible()) {
+                visibleEffectParticles.add(effect.getParticleOptions());
+            }
+        }
+        // Canvas end - remove streams
         this.entityData.set(DATA_EFFECT_PARTICLES, visibleEffectParticles);
-        this.entityData.set(DATA_EFFECT_AMBIENCE_ID, areAllEffectsAmbient(this.activeEffects.values()));
+        this.entityData.set(DATA_EFFECT_AMBIENCE_ID, areAllEffectsAmbient(effectsValues)); // Canvas - optimized collection
     }
 
     private void updateGlowingStatus() {
@@ -1850,12 +_,11 @@
     }
 
     public @Nullable DamageSource getLastDamageSource() {
-        if (this.level().getRedstoneGameTime() - this.lastDamageStamp > 40L || this.lastDamageStamp == Long.MIN_VALUE) { // Folia - region threading
-            this.lastDamageSource = null;
-        }
+        return this.getLastDamageSource(40);
+    }
 
     public @Nullable DamageSource getLastDamageSource(final int damageSourceTimeout) {
-        return this.level().getGameTime() - this.lastDamageStamp > damageSourceTimeout ? null : this.lastDamageSource;
+        return this.lastDamageStamp == Long.MIN_VALUE || this.level().getRedstoneGameTime() - this.lastDamageStamp > damageSourceTimeout ? null : this.lastDamageSource; // Canvas - use the migrated regional timestamp for every timeout
     }
 
     protected void playHurtSound(final DamageSource source) {
@@ -1927,6 +_,7 @@
                 if (sourceEntity == null || sourceEntity.killedEntity(serverLevel, this, source)) {
                     this.gameEvent(GameEvent.ENTITY_DIE);
                     this.postDeathEventTasks.forEach(Runnable::run); // Paper
+                    if (source.is(DamageTypes.WITHER_SKULL)) this.canvas$isDyingFromWitherRose = true; // Canvas - fix Folia#388
                     this.createWitherRose(killer);
                 }
 
@@ -1945,7 +_,7 @@
     protected void createWitherRose(final @Nullable LivingEntity killer) {
         if (this.level() instanceof ServerLevel serverLevel) {
             boolean var6 = false;
-            if (killer instanceof WitherBoss) {
+            if (killer instanceof WitherBoss || this.canvas$isDyingFromWitherRose) { // Canvas - fix Folia#388
                 if (serverLevel.getGameRules().get(GameRules.MOB_GRIEFING)) {
                     BlockPos pos = this.blockPosition();
                     BlockState state = Blocks.WITHER_ROSE.defaultBlockState();
@@ -2243,7 +_,7 @@
 
     @Override
     public boolean isAlive() {
-        return !this.isRemoved() && this.getHealth() > 0.0F && !this.dead; // Paper - Check this.dead
+        return !this.dead && !this.isRemoved() && this.getHealth() > 0.0F; // Paper - Check this.dead // Canvas - cheapest check first
     }
 
     public boolean isLookingAtMe(
@@ -2792,7 +_,7 @@
                 this.swapHandItems();
                 break;
             case 60:
-                this.makePoofParticles();
+                if (!level().canvasConfig().visuals.particles.disableDeathParticles) this.makePoofParticles(); // Canvas - particles config
                 break;
             case 65:
                 this.breakItem(this.getItemBySlot(EquipmentSlot.BODY));
@@ -3199,7 +_,7 @@
             waterWalker *= 0.5F;
         }
 
-        if (waterWalker > 0.0F) {
+        if (waterWalker > 0.0F && (!io.canvasmc.canvas.GlobalConfiguration.getInstance().vanillaFixes.mc136249 || autoSpinAttackTicks == 0)) { // Canvas - MC-136249
             slowDown += (0.54600006F - slowDown) * waterWalker;
             speed += (this.getSpeed() - speed) * waterWalker;
         }
@@ -3848,7 +_,7 @@
             this.checkAutoSpinAttack(beforeTravelBox, this.getBoundingBox());
         }
 
-        this.pushEntities();
+        if (level().canvasConfig().entities.entityCollisionMode.allEntitiesCanBePushed()) this.pushEntities(); // Canvas - entity collision mode config
         // Paper start - Add EntityMoveEvent
         if (((ServerLevel) this.level()).getCurrentWorldData().hasEntityMoveEvent && !(this instanceof Player)) { // Folia - region threading
             if (this.xo != this.getX() || this.yo != this.getY() || this.zo != this.getZ() || this.yRotO != this.getYRot() || this.xRotO != this.getXRot()) {
@@ -3884,6 +_,7 @@
     protected void updateFallFlying() {
         this.checkFallDistanceAccumulation();
         if (!this.level().isClientSide()) {
+            if (!this.isFallFlying() && this.fallFlyTicks == 0) return; // Canvas - skip unnecessary calculations if player is not flying
             if (!this.canGlide()) {
                 if (this.getSharedFlag(Entity.FLAG_FALL_FLYING) && !CraftEventFactory.callToggleGlideEvent(this, false).isCancelled()) // CraftBukkit
                 this.setSharedFlag(Entity.FLAG_FALL_FLYING, false);
@@ -3922,6 +_,12 @@
     }
 
     protected void pushEntities() {
+        // Canvas start - only players push config
+        io.canvasmc.canvas.WorldConfig.Entities.EntityCollisionMode collisionMode = level().canvasConfig().entities.entityCollisionMode;
+        if (collisionMode.noCollisions()) {
+            return;
+        }
+        // Canvas end - only players push config
         // Paper start - don't run getEntities if we're not going to use its result
         if (!this.isPushable()) {
             return;
@@ -3937,7 +_,11 @@
             return;
         }
         // Paper end - don't run getEntities if we're not going to use its result
-        List<Entity> pushableEntities = this.level().getPushableEntities(this, this.getBoundingBox());
+        // Canvas start - only players push config
+        List<? extends Entity> pushableEntities = collisionMode.onlyPlayersPushable() ?
+            this.level().canvas$getIntersectingPlayers(this, this.getBoundingBox(), EntitySelector.pushableBy(this))
+            : this.level().getPushableEntities(this, this.getBoundingBox());
+        // Canvas end - only players push config
         if (!pushableEntities.isEmpty()) {
             if (this.level() instanceof ServerLevel serverLevel) {
                 // Paper - don't run getEntities if we're not going to use its result; moved up
@@ -4124,6 +_,7 @@
 
     @Override
     public boolean isCollidable(boolean ignoreClimbing) {
+        if (this.canvas$fromSpawner && level().canvasConfig().blocks.spawner.spawnedEntitiesHaveNoCollision) return false; // Canvas - spawner configs
         // Paper end - Climbing should not bypass cramming gamerule
         return (this.isAlwaysTicking() || !(this.level() instanceof ServerLevel level && !level.isPositionEntityTicking(this.blockPosition())))
             && this.isAlive()
@@ -4246,6 +_,18 @@
             );
         }
 
+        // Canvas start - item entity spread config
+        if (this.isDeadOrDying()) {
+            float power = this.random.nextFloat() * 0.5F;
+            float direction = this.random.nextFloat() * (float) (Math.PI * 2);
+            double factor = level().canvasConfig().entities.itemEntities.itemEntityVelocityOnDeathFactor;
+            entity.setDeltaMovement(
+                (-Mth.sin(direction) * power) * factor,
+                0.2F * factor,
+                (Mth.cos(direction) * power) * factor
+            );
+        }
+        // Canvas end - item entity spread config
         return entity;
     }
 
@@ -4578,39 +_,25 @@
         // Paper end - teleport cause API
         double y = yy;
         Level level = this.level();
-        if (ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor((ServerLevel)level, pos) && level.hasChunkAt(pos)) { // Folia - region threading
-            boolean landed = false;
-
-            while (!landed && pos.getY() > level.getMinY()) {
-                BlockPos below = pos.below();
-                BlockState state = level.getBlockState(below);
-                if (state.blocksMotion()) {
-                    landed = true;
-                } else {
-                    y--;
-                    pos = below;
-                }
-
-            if (landed) {
-                // CraftBukkit start - Teleport event
-                // first set position, to check if the place to teleport is valid
-                this.setPos(xx, y, zz);
-                if (level.noCollision(this) && !level.containsAnyLiquid(this.getBoundingBox())) {
-                    ok = true;
-                }
-                // now revert and call event if the teleport place is valid
-                this.setPos(xo, yo, zo);
-
-                if (ok) {
-                    // Canvas start - region threading
-                    boolean teleportResult = this.teleportAsync(
-                        (ServerLevel) this.level(), new Vec3(xx, y, zz), this.getYRot(), this.getXRot(), this.getDeltaMovement(), cause, Entity.TELEPORT_FLAG_LOAD_CHUNK | Entity.TELEPORT_FLAG_UNMOUNT, null
-                    );
-                    if (!teleportResult) return Optional.empty();
-                    // Canvas end - region threading
-                }
-                // CraftBukkit end
-            }
+        WorldBorder worldBorder = level.getWorldBorder();
+        Vec3 clamped = worldBorder.clampVec3ToBound(xx, y, zz);
+        BlockPos.MutableBlockPos pos = BlockPos.containing(clamped).mutable();
+        if (ca.spottedleaf.moonrise.common.util.TickThread.isTickThreadFor((ServerLevel) level, pos) && level.hasChunkAt(pos)) { // Folia - region threading
+            // Canvas start - reuse the loaded destination chunk during the vertical scan
+            net.minecraft.world.level.chunk.LevelChunk chunk = level.getChunkIfLoaded(pos);
+            if (chunk == null) {
+                return RandomTeleportResult.FAILURE;
+            }
+            while (pos.getY() > level.getMinY()) {
+                pos.move(Direction.DOWN);
+                BlockState state = chunk.getBlockState(pos);
+                if (state.is(BlockTags.ENTITIES_CAN_TELEPORT_TO)) {
+                    return this.checkPositionAndTeleport(clamped.x, y, clamped.z, showParticles, isInvalidPosition, state, level, cause); // Paper - teleport cause API
+                }
+
+                y--;
+            }
+            // Canvas end - reuse the loaded destination chunk during the vertical scan
         }
 
         return RandomTeleportResult.FAILURE; // Paper - teleport event
@@ -4638,30 +_,22 @@
             }
 
             if (this.canRandomlyTeleportTo(xx, y, zz)) {
-                // Paper start - teleport event
-                if (this instanceof net.minecraft.server.level.ServerPlayer player) {
-                    // player teleport event is called in the underlining code
-                    if (!player.connection.teleport(xx, y, zz, this.getYRot(), this.getXRot(), cause)) {
-                        return net.minecraft.world.entity.LivingEntity.RandomTeleportResult.EVENT_CANCELLED;
-                    }
-                } else {
-                    org.bukkit.event.entity.EntityTeleportEvent teleport = new org.bukkit.event.entity.EntityTeleportEvent(this.getBukkitEntity(), org.bukkit.craftbukkit.util.CraftLocation.toBukkit(this.oldPosition(), this.level()), new org.bukkit.Location(this.level().getWorld(), xx, y, zz));
-                    this.level().getCraftServer().getPluginManager().callEvent(teleport);
-                    if (!teleport.isCancelled() && teleport.getTo() != null) { // Paper
-                        org.bukkit.Location to = teleport.getTo();
-                        this.teleportTo(to.getX(), to.getY(), to.getZ());
-                    } else {
-                        return net.minecraft.world.entity.LivingEntity.RandomTeleportResult.EVENT_CANCELLED;
-                    }
-                }
-                // Paper end - teleport event
-                if (showParticles) {
-                    level.broadcastEntityEvent(this, EntityEvent.TELEPORT);
-                }
-
-                if (this instanceof PathfinderMob pathfinderMob) {
-                    pathfinderMob.getNavigation().stop();
-                }
+                // Canvas start - preserve region-safe random teleports, including plugin redirects
+                boolean accepted = this.teleportAsync(
+                    (ServerLevel) level, new Vec3(xx, y, zz), this.getYRot(), this.getXRot(), this.getDeltaMovement(), cause,
+                    Entity.TELEPORT_FLAG_LOAD_CHUNK | Entity.TELEPORT_FLAG_UNMOUNT, entity -> {
+                        if (showParticles) {
+                            entity.level().broadcastEntityEvent(entity, EntityEvent.TELEPORT);
+                        }
+                        if (entity instanceof PathfinderMob pathfinderMob) {
+                            pathfinderMob.getNavigation().stop();
+                        }
+                    }
+                );
+                if (!accepted) {
+                    return RandomTeleportResult.EVENT_CANCELLED;
+                }
+                // Canvas end - preserve region-safe random teleports, including plugin redirects
 
                 return RandomTeleportResult.SUCCESS; // Paper - teleport event
             } else {
@@ -5085,6 +_,7 @@
         }
 
         public void tick() {
+            if (this.currentSwing == null && this.oldAnimation == 0.0F && this.animation == 0.0F) return; // Canvas - skip unnecessary calculations if player is not flying or swing
             this.oldAnimation = this.animation;
             if (this.currentSwing != null) {
                 if (this.currentSwing.durationTicks > 0) {
